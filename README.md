After decompression, place the folder into the res://vcder/ path. Add three new nodes: SoundPlayerVc2D, SoundPlayerVc, and MusicPlayerVc.
Load audio into sounds_stream, and use the vc_play() method to play according to the selected pattern (default is random).

解压后将文件夹放至res://vcder/ 路径下,新增SoundPlayerVc2D,SoundPlayerVc,MusicPlayerVc 三个新节点。为sounds_stream装填音频，
使用vc_play()方法即可按照选择的规律播放(默认随机)
